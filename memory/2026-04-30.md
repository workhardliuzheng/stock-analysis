# 2026-04-30 - 股票分析系统 V20 ML融合实验

## 事件记录

### 信号融合规则ML化实验 (V20尝试)

**实施内容**:
- 创建 `analysis/ml_signal_fuser.py` — XGBoost分类器(3种子集成)
- 修改 `signal_generator.py` — 支持 use_ml_fusion 参数
- 修改 `index_analyzer.py` — MLSignalFuser调用
- 修改 `adaptive_fusion_optimizer.py` — MetaLearner加入第5维ml_fused_signal权重
- 全链路参数传递: main.py → portfolio_backtest → PortfolioBacktester → IndexAnalyzer
- CLI: --no-ml-fusion

**回测结果 (2020-01 ~ 2026-04, --no-defense)**:
- ML融合: +60.49%, 最大回撤 -26.21%, 夏普 0.44
- 规则基线: +78.49%, 最大回撤 -24.50%, 夏普 0.53
- 差异: -18.00pp, 回滚

**问题分析**:
- ML融合在深证成指(-4.56pp)和科创50(-9.63pp)产生噪音
- V7-5 MetaLearner共识投票抵消了ML融合的积极信号
- 5日前瞻标签在震荡市产生错误标签

**结论**: 回退if-else规则，当前规则足够健壮

**回滚操作**:
- `git checkout` 恢复5个文件
- 删除临时脚本(ml_signal_fuser.py, _test_fuser.py, _find_*.py等)
- 更新 AGENTS.md, TODO.md

### 文档更新
- TODO.md: 标记"信号融合规则优化"为"ML方案尝试失败"
- AGENTS.md: 新增"27. 信号融合规则ML化尝试"章节
- memory/2026-04-30.md: 本文件

### 后续方向
- 需重新设计ML融合方案再尝试
- P1: Regime状态特征增强

### V21 Regime状态特征增强 (2026-04-30 下午)

**实施内容**:
1. **Regime持续时长特征** (`regime_duration`)
   - `market_regime_detector.py`: `_calc_regime_duration()` 按状态分组累计计数
   - 测试: labels `['A','A','B','B','B','A','A','A','C','C']` → duration `[1,2,1,2,3,1,2,3,1,2]`
   - 实盘: SIDEWAYS均值44.7d, HIGH_VOL均值3.9d

2. **Regime转换概率特征** (`regime_transition_stay`, `regime_transition_flip`)
   - `market_regime_detector.py`: `_calc_transition_prob_by_target()` 滚动60天窗口
   - 测试: stay+flip≈100自洽
   - 实盘: SIDEWAYS停留率98.8%, HIGH_VOL翻转率50.0%

3. **跨指数Regime一致性特征** (`feat_cross_50_regime_consistency`)
   - `index_analyzer.py`: 在`_apply_cross_index_features()`中计算上证50 vs 当前指数的regime一致性
   - 评分: 20(牛熊互斥)/50(部分一致)/70(同类不同级)/90(完全一致)
   - 实盘: 65.4%完全一致, 仅0.6%牛熊互斥

4. **ML特征集成**: `ml_predictor.py`的`prepare_features()`新增3个特征列

**回测结果**: +74.71%, 回撤-18.64%, 夏普0.57
- vs V18基线 +78.49%, -24.50%, 0.53
- 收益率持平(-3.78pp)，回撤改善5.86pp

**方向准确率**: ~52.8% (基线~53.7%, 持平)

**清理**: 删除了所有测试脚本(_test_*.py, _run_backtest.py, _read_output.py)

**Git提交**: (待提交)

